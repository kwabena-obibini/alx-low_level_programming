#include "main.h"

/**
 * _abs - computing the value of an integer
 * @n: running the check
 * Return: the absolute value of int
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
